# Copy to .env.local (gitignored) for local dev.
# Free tier, no credit card: https://www.helius.dev/
# Without this set, Solana SPL/Token-2022 balance lookups (getTokenAccountsByOwner)
# will 403 on the public fallback RPC host — see src/lib/chains/solana/solanaRpcClient.ts.
VITE_HELIUS_API_KEY=
